enum HomeTabType {
  all,
  compeleted,
  uncompleted,
  favorite,
}

enum PriorityType {
  critical,
  high,
  medium,
  low,
}

enum ReminderType {
  tenMins,
  twentyMins,
  oneHour,
}

enum RepeatType {
  daily,
  weakly,
  monthly,
}
